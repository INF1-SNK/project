import React from 'react';
import './icon.css';

/**
 * Primary UI component for user interaction
 */
export const Button = ({image, size}) => {
  const mode = primary ? 'storybook-button--primary' : 'storybook-button--secondary';
  return (
    <a></a>
  );
};

